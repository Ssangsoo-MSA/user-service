server:
  port: 9001

spring:
  application:
    name: user-service

eureka:
  client:
    register-with-eureka: true # EUREKA 서버에 이 애플리케이션을 등록할지 여부
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka